# Core
numpy>=1.24.0
torch>=2.1.0
torch-directml>=0.2.0; sys_platform == "win32"

# Data pipeline (Stage 1-3)
datasets>=2.14.0
transformers>=4.35.0
tokenizers>=0.15.0
huggingface_hub>=0.19.0

# Optional but recommended
sentencepiece>=0.1.99
protobuf>=3.20.0
accelerate>=0.25.0

# Training utilities
tqdm
safetensors>=0.4.0

# CUDA build (optional, Linux only on this machine)
# ninja
